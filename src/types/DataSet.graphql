type DataSet {
  id: ID!
  name: ID!
  description: String!
  licence: String!
  url: String!
  version: String
  synopsis: String
  dataSource: DataSource!
  publication: Publication
  bioEntities: [BioEntity!]!
}

interface HasDataSets {
  dataSets: [DataSet!]!
}
