type Strain
#implements HasDataSets & HasOrganism
{
  id: ID!
  identifier: ID!
  name: String
  description: String
  origin: String
  accession: String
  # HasDataSets
  dataSets: [DataSet!]!
  # HasOrganism
  organism: Organism
}

#interface HasStrain {
#  strain: Strain
#}
