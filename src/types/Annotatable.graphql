interface Annotatable implements HasDataSets & HasOntologyAnnotations & HasPublications {
  identifier: ID!
  # HasDataSets
  dataSets: [DataSet!]!
  # HasOntologyAnnotations
  ontologyAnnotations: [OntologyAnnotation!]!
  # HasPublications
  publications: [Publication!]!
}
