type Phylotree implements Annotatable & HasDataSets & HasOntologyAnnotations & HasPublications {
  id: ID!
  # Annotatable
  identifier: ID!
  dataSets: [DataSet!]!
  ontologyAnnotations: [OntologyAnnotation!]!
  publications: [Publication!]!
  # Phylotree
  numLeaves: Int
  geneFamily: GeneFamily
  newick: String
  nodes: [Phylonode!]!
}
