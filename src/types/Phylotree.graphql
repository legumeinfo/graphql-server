type Phylotree implements Annotatable & HasDataSets & HasPublications {
  id: ID!
  # Annotatable
  identifier: ID!
  ontologyAnnotations: [OntologyAnnotation!]!
  dataSets: [DataSet!]!
  publications: [Publication!]!
  # Phylotree
  numLeaves: Int
  geneFamily: GeneFamily
  newick: String
  nodes: [Phylonode!]!
}
