type SyntenyBlock implements Annotatable
#& HasDataSets & HasOntologyAnnotations & HasPublications
{
  id: ID!
  # Annotatable
  identifier: ID!
  dataSets: [DataSet!]!
  ontologyAnnotations: [OntologyAnnotation!]!
  publications: [Publication!]!
  # SyntenyBlock
  medianKs: Float
  syntenicRegions: [SyntenicRegion!]!
}
