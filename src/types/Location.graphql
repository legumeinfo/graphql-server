type Location implements HasDataSets {
  id: ID!
  strand: String
  start: Int
  end: Int
  chromosome: Chromosome
  supercontig: Supercontig
  # feature: BioEntity
  linkouts: [Linkout!]!
  ## HasDataSets
  dataSets: [DataSet!]!
}
