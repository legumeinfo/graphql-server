type Location implements HasDataSets {
  id: ID!
  strand: String
  start: Int
  end: Int
  locatedOn: BioEntity
  #feature: BioEntity
  linkouts: [Linkout!]!
  ## HasDataSets
  dataSets: [DataSet!]!
}
