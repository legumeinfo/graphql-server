type ExpressionSample implements Annotatable & HasDataSets & HasPublications {
  id: ID!
  # Annotatable
  identifier: ID!
  ontologyAnnotations: [OntologyAnnotation!]!
  dataSets: [DataSet!]!
  publications: [Publication!]!
  # ExpressionSample
  tissue: String
  num: Int
  description: String
  replicateGroup: String
  treatment: String
  bioSample: String
  sraExperiment: String
  species: String
  genotype: String
  name: String
  developmentStage: String
  source: ExpressionSource
}
