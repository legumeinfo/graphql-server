interface OntologyTerm implements HasDataSets & HasOntology & HasOntologyAnnotations {
  id: ID!
  ## OntologyTerm
  identifier: ID!
  description: String
  obsolete: Boolean
  name: String
  namespace: String
  # relations
  # synonyms
  parents: [OntologyTerm!]!
  crossReferences: [OntologyTerm!]!
  # HasOntology
  ontology: Ontology
  ## HasOntologyAnnotations
  ontologyAnnotations: [OntologyAnnotation!]!
  ## HasDataSets
  dataSets: [DataSet!]!
}
