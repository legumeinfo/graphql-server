type GeneFamily implements HasGenes & HasPhylotree & HasProteinDomains & Annotatable & HasDataSets & HasOntologyAnnotations & HasPublications {
  id: ID!
  # Annotatable
  identifier: ID!
  dataSets: [DataSet!]!
  ontologyAnnotations: [OntologyAnnotation!]!
  publications: [Publication!]!
  # HasGenes
  genes: [Gene!]!
  # HasPhylotree
  phylotree: Phylotree
  # HasProteinDomains
  proteinDomains: [ProteinDomain!]!
  # GeneFamily
  description: String
  size: Int
  tallies: [GeneFamilyTally!]!
  version: String
  ## microservices
  linkouts: [Linkout!]!
}

interface HasGeneFamily {
  geneFamily: GeneFamily
}
