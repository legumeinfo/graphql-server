type GeneFamily implements HasPhylotree & Annotatable & HasDataSets & HasOntologyAnnotations & HasPublications {
  id: ID!
  # Annotatable
  identifier: ID!
  dataSets: [DataSet!]!
  ontologyAnnotations: [OntologyAnnotation!]!
  publications: [Publication!]!
  # HasPhylotree
  phylotree: Phylotree
  # GeneFamily
  description: String
  genes: [Gene!]!
  proteinDomains: [ProteinDomain!]!
  size: Int
  tallies: [GeneFamilyTally!]!
  version: String
  ## microservices
  linkouts: [Linkout!]!
}

interface HasGeneFamily {
  geneFamily: GeneFamily
}
