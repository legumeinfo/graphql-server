type PanGeneSet implements HasProteins & HasTranscripts & Annotatable & HasDataSets & HasOntologyAnnotations & HasPublications {
  id: ID!
  # Annotatable
  identifier: ID!
  dataSets: [DataSet!]!
  ontologyAnnotations: [OntologyAnnotation!]!
  publications: [Publication!]!
  # HasProteins
  proteins: [Protein!]!
  # HasTranscripts
  transcripts: [Transcript!]!
  # HasGenes
  genes: [Gene!]!
  # microservices
  linkouts: [Linkout!]!
}

interface HasPanGeneSets {
  panGeneSets: [PanGeneSet!]!
}
