type LinkageGroup implements Annotatable & HasDataSets & HasPublications {
  id: ID!
  # Annotatable
  identifier: ID!
  ontologyAnnotations: [OntologyAnnotation!]!
  dataSets: [DataSet!]!
  publications: [Publication!]!
  # LinkageGroup
  name: String
  length: Float
  number: Int
  geneticMap: GeneticMap
  qtls: [QTL!]!
}
