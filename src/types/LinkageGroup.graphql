type LinkageGroup implements Annotatable & HasDataSets & HasOntologyAnnotations & HasPublications {
  id: ID!
  # Annotatable
  identifier: ID!
  dataSets: [DataSet!]!
  ontologyAnnotations: [OntologyAnnotation!]!
  publications: [Publication!]!
  # LinkageGroup
  name: String
  length: Float
  number: Int
  geneticMap: GeneticMap
  qtls: [QTL!]!
}
