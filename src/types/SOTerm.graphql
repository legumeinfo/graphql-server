# OntologyTerm is a type, not interface, so we just copy it here
type SOTerm implements OntologyTerm & HasDataSets & HasOntologyAnnotations {
  id: ID!
  ## OntologyTerm
  identifier: ID!
  description: String
  obsolete: Boolean
  name: String
  namespace: String
  ontology: Ontology
  # relations
  # synonyms
  parents: [OntologyTerm!]!
  crossReferences: [OntologyTerm!]!
  ## HasDataSets
  dataSets: [DataSet!]!
  ## HasOntologyAnnotations
  ontologyAnnotations: [OntologyAnnotation!]!
}


